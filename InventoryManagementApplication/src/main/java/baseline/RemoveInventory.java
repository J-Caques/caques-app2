package baseline;

public class RemoveInventory {
    /*
        Method that removes a single item from the list
        removeItem(){
        Prompt User for item they which to delete
        String delete = input.line()

        Look up index of that item
        int index of Arrays.indexOf(item)
        Inventory.remove(index)

        }
     */
}
